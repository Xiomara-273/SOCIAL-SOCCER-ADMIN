import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.socialsoccer.admin',
  appName: 'Social Soccer Admin',
  webDir: 'dist/social-soccer-clean/browser',
};

export default config;
